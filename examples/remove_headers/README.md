The Lua filter is injected to remove headers at ingress.

To install the Lua filter, please invoke the following commands:

```bash
$ git clone git@github.com:istio/tools.git
$ cd tools/examples/remove_headers/
$ ./setup.sh
```
